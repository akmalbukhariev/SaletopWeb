export type AnnouncementType = {
    announcement_id: number;
    preview: string;
    created_at_utc: number;
    is_active: boolean;
    image_url: string;
    scope: string;
    title: string;
    body: string;
}